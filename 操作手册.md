# 财务RPA 加载宏 — 操作手册

---

## 目录

- [1. 系统概述](#1-系统概述)
- [2. 系统要求](#2-系统要求)
- [3. 文件结构](#3-文件结构)
- [4. 工作流总览](#4-工作流总览)
- [5. 当前状态](#5-当前状态)
- [6. 日常操作](#6-日常操作)
  - [6.1 修改现有宏代码](#61-修改现有宏代码)
  - [6.2 新增宏 + 新增按钮](#62-新增宏--新增按钮)
  - [6.3 删除按钮](#63-删除按钮)
  - [6.4 调整布局](#64-调整布局)
- [7. 构建说明](#7-构建说明)
- [8. 打包分发](#8-打包分发)
  - [8.1 打包](#81-打包)
  - [8.2 安装（目标电脑）](#82-安装目标电脑)
  - [8.3 更新（已有旧版）](#83-更新已有旧版)
  - [8.4 卸载](#84-卸载)
- [9. 模板与参考](#9-模板与参考)
  - [9.1 .bas 模板](#91-bas-模板)
  - [9.2 按钮代码块模板](#92-按钮代码块模板)
  - [9.3 按钮参数说明](#93-按钮参数说明)
  - [9.4 常用图标编号表](#94-常用图标编号表)
- [10. 注意事项](#10-注意事项)
- [11. 常见问题排查](#11-常见问题排查)
- [12. 快速参考卡](#12-快速参考卡)

---

## 1. 系统概述

将 VBA 宏代码封装为 WPS 加载宏（.xlam），通过「加载项」选项卡提供按钮操作入口。支持代码维护、构建打包、跨电脑分发的一体化工作流。

**核心能力：**

- VBA 代码以纯文本文件管理（.bas），无需打开 VBA 编辑器
- 一键构建（build.vbs）将源代码编译为加载宏
- 一键打包（pack.vbs）生成分发安装包
- 目标电脑双击安装，无需任何开发环境

**三步口诀：**

```
改代码 → 关 WPS → 双击 build.vbs
双击 pack.vbs → 生成安装包
复制安装包 → 双击 install.vbs
```

---

## 2. 系统要求

### 开发电脑（维护方）

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 |
| WPS 表格 | 2019 及以上 |
| PowerShell | 系统自带（Win10/11 默认有） |
| 信任设置 | WPS 选项 → 信任中心 → 宏设置 → 勾选「信任对 VBA 工程对象模型的访问」 |
| VBA 密码 | 无（VBA 工程不能有密码保护） |

### 目标电脑（使用方）

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 |
| WPS 表格 | 2019 及以上 |
| 其他 | 无 |

---

## 3. 文件结构

```
VBA代码/
│
├── 源代码 (src/)                     ← 日常编辑这里
│   ├── ThisWorkbook.cls              ← 启动事件（调用工具栏创建，5行）
│   ├── Module2.bas                   ← 库存价值表数据清洗
│   ├── Module3.bas                   ← 清洗库龄表数据
│   ├── Module4.bas                   ← 库龄表期初变化分析
│   ├── Module5.bas                   ← 采购发票信息数据清洗
│   ├── Module6.bas                   ← 固定资产隐藏列（Sub: btnHideFixedAssetColumns）
│   ├── Module7.bas                   ← 工具栏创建（AutoSetupToolbar）
│   └── 按钮模板.txt                   ← 新建按钮时参考此模板
│
├── 构建工具
│   ├── build.vbs                     ← 双击构建（主构建脚本）
│   └── fix_isaddin.ps1               ← 修补 isAddin 属性（构建时自动调用）
│
├── 打包工具
│   └── pack.vbs                      ← 双击生成安装包
│
├── 成品文件
│   ├── FinanceRPA.xlam               ← 加载宏（构建后生成，不要手动改）
│   └── FinanceRPA安装包/              ← 安装包（打包后生成）
│       ├── install.vbs               ← 一键安装脚本
│       ├── FinanceRPA.xlam           ← 加载宏副本
│       └── 使用说明.txt               ← 安装说明
│
└── 操作手册.md                       ← 本文件
```

---

## 4. 工作流总览

```
┌─────────────────────────────────────────────────────────┐
│                    日常维护与分发流程                       │
│                                                         │
│  编辑 src/*.bas ──→ 双击 build.vbs ──→ 双击 pack.vbs     │
│       │                  │                  │           │
│       │           FinanceRPA.xlam    FinanceRPA安装包/   │
│       │              (本地生效)          (可分发)        │
│       │                                    │            │
│       │                              复制到 U盘/网络     │
│       │                                    │            │
│       │                              目标电脑双击        │
│       │                              install.vbs        │
│       │                                    │            │
│       └───────────── 重启 WPS ←─── 安装完成 ←──┘        │
└─────────────────────────────────────────────────────────┘
```

---

## 5. 当前状态

- **加载宏文件**：`FinanceRPA.xlam`（已注册到 WPS，启动时自动加载）
- **选项卡位置**：「加载项」选项卡
- **布局**：每行 2 个按钮，带图标
- **按钮清单**：

| 序号 | 按钮名称 | 图标编号 | OnAction | 对应模块 |
|------|---------|---------|----------|---------|
| 1 | 库存价值表清洗 | 210 | 库存价值表数据清洗 | Module2.bas |
| 2 | 库龄表清洗 | 210 | 清洗库龄表数据 | Module3.bas |
| 3 | 库龄期初变化分析 | 129 | 库龄表期初变化分析 | Module4.bas |
| 4 | 采购发票清洗 | 210 | 采购发票信息数据清洗 | Module5.bas |
| 5 | 固定资产隐藏列 | 210 | btnHideFixedAssetColumns | Module6.bas |
| 6 | 关于 | 487 | OnBtnAbout | Module7.bas |

> **注意**：按钮 3、5、6 设有 `BeginGroup = True`，会换行显示。

---

## 6. 日常操作

### 6.1 修改现有宏代码

**步骤：**

1. 用记事本或 VS Code 打开 `src/` 下对应的 .bas 文件
2. 修改代码，保存（文件编码：UTF-8）
3. 关闭 WPS
4. 双击 `build.vbs` → 等待完成弹窗
5. 重新打开 WPS → 「加载项」选项卡 → 测试按钮

**示例：**

将库龄表清洗的呆滞判定天数从 180 改成 150：

1. 打开 `src/Module3.bas`
2. 找到 `180` 改为 `150`，保存
3. 关闭 WPS → 双击 `build.vbs`
4. 重开 WPS → 测试按钮

### 6.2 新增宏 + 新增按钮

#### 第 1 步：创建 .bas 文件

在 `src/` 目录新建文件，如 `Module8.bas`，内容：

```vb
Attribute VB_Name = "Module8"
Sub 应付账款清洗()
    ' 你的代码写这里
    Dim ws As Worksheet
    Set ws = ActiveSheet
    ' ...
    MsgBox "完成", vbInformation
End Sub
```

**关键要求：**
- 第一行 `Attribute VB_Name = "模块名"` 必须有
- 模块名不能与现有模块重复
- Sub 名称不能与其他 .bas 文件中的 Sub 重名
- Sub 名称建议用中文（构建脚本会自动处理编码），但也可用 ASCII 字符
- 文件保存为 UTF-8 编码（记事本默认就是，VS Code 需确认右下角显示 UTF-8）

#### 第 2 步：在 Module7.bas 中添加按钮

用记事本打开 `src/Module7.bas`，找到 `cb.Visible = True`（在 `AutoSetupToolbar` 末尾），在其**前面**插入按钮代码块：

```vb
    ' 按钮7: 应付账款清洗
    Set btn = cb.Controls.Add(1)
    btn.Caption = "应付账款清洗"        ' 按钮显示名
    btn.OnAction = "应付账款清洗"        ' 必须与 Sub 名称完全一致
    btn.Style = 3                       ' 3=图标+文字, 1=仅文字
    btn.FaceId = 2156                   ' 图标编号
    btn.Width = 120                     ' 按钮宽度
    btn.BeginGroup = True               ' True=换行显示, False=接着上一个
    Err.Clear
```

**关键要求：**
- `btn.OnAction` 的值必须与 .bas 中的 `Sub` 名称完全一致（包括中英文、大小写）
- `btn.BeginGroup = True` 表示换行显示
- 按钮代码块在 Module7.bas 中的顺序决定按钮排列顺序

#### 第 3 步：构建

关闭 WPS → 双击 `build.vbs` → 等完成 → 重开 WPS

### 6.3 删除按钮

1. 删除 `src/` 下对应的 .bas 文件
2. 打开 `src/Module7.bas`，删除对应的按钮代码块（从注释行到 `Err.Clear`）
3. 关闭 WPS → 双击 `build.vbs` → 重开 WPS

### 6.4 调整布局

打开 `src/Module7.bas`，修改对应按钮的参数：

| 调整内容 | 参数 | 取值 | 说明 |
|---------|------|------|------|
| 换行 | `btn.BeginGroup` | `True` / `False` | True = 新起一行 |
| 宽度 | `btn.Width` | 数字 | 120 适合 4 字按钮，160 适合 6 字 |
| 图标 | `btn.FaceId` | 数字 | 见[图标编号表](#94-常用图标编号表) |
| 显示文字 | `btn.Caption` | 字符串 | 按钮上显示的名称 |
| 显示样式 | `btn.Style` | `1` / `3` | 1=仅文字，3=图标+文字 |

改完保存 → 关闭 WPS → 双击 `build.vbs` → 重开 WPS

---

## 7. 构建说明

### 构建脚本自动执行的步骤

1. **编码转换** — 将 .bas 文件从 UTF-8 转为 GBK（WPS VBA 需要 GBK）
2. **创建新工作簿** — 从空白工作簿开始，确保 VBA 修改能持久化
3. **清理默认模块** — 移除 Sheet1 等默认模块的代码
4. **写入 ThisWorkbook** — 5 行启动代码（调用 AutoSetupToolbar）
5. **导入 .bas** — 将所有 .bas 文件导入 VBA 工程
6. **保存 .xlsm** — 先存为 .xlsm 格式（VBA 修改持久化到磁盘）
7. **SaveAs .xlam** — 用格式 55（xlOpenXMLAddIn）另存为 .xlam
8. **修补 isAddin** — 调用 fix_isaddin.ps1 在 ZIP 内部 workbook.xml 中添加 `isAddin="true"`（WPS SaveAs 不自动写入此属性）
9. **部署** — 复制 .xlam 到 WPS 启动文件夹

### 运行方式

双击 `build.vbs` 即可。

### 构建前提

- WPS 必须关闭（否则文件被锁定，构建会失败）
- WPS 信任设置已开启（信任 VBA 工程对象模型）
- VBA 工程无密码
- PowerShell 可用（Win10/11 默认自带）

---

## 8. 打包分发

### 8.1 打包

1. 确认已运行过 `build.vbs`（生成了最新的 FinanceRPA.xlam）
2. 双击 `pack.vbs`
3. 等待完成弹窗
4. 安装包生成在 `FinanceRPA安装包/` 目录

安装包内容：

| 文件 | 用途 |
|------|------|
| install.vbs | 一键安装脚本 |
| FinanceRPA.xlam | 加载宏 |
| 使用说明.txt | 安装说明 |

### 8.2 安装（目标电脑）

1. 将整个 `FinanceRPA安装包/` 文件夹复制到目标电脑（U盘、网络共享均可）
2. 关闭 WPS
3. 双击 `install.vbs`
4. 安装脚本自动完成：
   - 设置 WPS 信任 VBA 工程
   - 复制 .xlam 到 WPS 启动文件夹
   - 注册加载宏
5. 打开 WPS 表格 → 「加载项」选项卡 → 确认按钮出现

**验证安装：**
1. 打开 WPS 表格（新建空白表格或打开任意文件）
2. 查看功能区是否有「加载项」选项卡
3. 点击「加载项」应看到「FinanceRPA」工具组
4. 点击任意按钮测试功能

**如果看不到按钮：**
1. 右键功能区 → 自定义功能区
2. 勾选「加载项」选项卡
3. 确定后重新打开 WPS

### 8.3 更新（已有旧版）

直接重新运行 `install.vbs`，会覆盖旧版本。无需先卸载。

### 8.4 卸载

1. 关闭 WPS
2. 删除启动文件夹中的 FinanceRPA.xlam
   （路径通常为：`C:\Users\用户名\AppData\Roaming\Kingsoft\office6\startup\`）
3. 重新打开 WPS，选项卡消失

---

## 9. 模板与参考

### 9.1 .bas 模板

新建 .bas 文件时，参考此模板：

```vb
Attribute VB_Name = "Module8"
Sub 宏名称()
    ' 替换为你自己的代码
    Dim ws As Worksheet
    Set ws = ActiveSheet

    ' 你的逻辑写这里

    MsgBox "完成", vbInformation
End Sub
```

### 9.2 按钮代码块模板

新建按钮时，复制以下代码块到 `src/Module7.bas` 的 `AutoSetupToolbar` 中（`cb.Visible = True` 之前）：

```vb
    ' 按钮(N+1): 功能名称
    Set btn = cb.Controls.Add(1)
    btn.Caption = "功能名称"          ' 按钮显示文字
    btn.OnAction = "宏名称"            ' 必须与 .bas 里的 Sub 名称完全一致
    btn.Style = 3                     ' 3=图标+文字, 1=仅文字
    btn.FaceId = 489                   ' 图标编号（见下方图标表）
    btn.Width = 120                    ' 按钮宽度
    btn.BeginGroup = True              ' True=换行显示, False=接着上一个
    Err.Clear
```

### 9.3 按钮参数说明

| 参数 | 作用 | 常用值 |
|------|------|--------|
| `btn.Caption` | 按钮显示文字 | 任意字符串 |
| `btn.OnAction` | 点击时调用的宏名 | 必须与 Sub 名称完全一致 |
| `btn.Style` | 显示样式 | `1`=仅文字，`3`=图标+文字 |
| `btn.FaceId` | 图标编号 | 见[图标表](#94-常用图标编号表) |
| `btn.Width` | 按钮宽度 | `120`（4字）/ `160`（6字） |
| `btn.BeginGroup` | 是否换行 | `True`=换行，`False`=不换行 |

### 9.4 常用图标编号表

| 编号 | 图标 | 适用场景 |
|------|------|---------|
| 489 | 表格 | 数据表处理 |
| 210 | 筛选漏斗 | 数据清洗 |
| 129 | 日历 | 时间/期初分析 |
| 2156 | 求和 Sigma | 金额/财务 |
| 95 | 柱状图 | 图表分析 |
| 593 | 计算器 | 计算 |
| 487 | 问号 | 帮助/关于 |
| 3 | 软盘 | 保存操作 |
| 4 | 打印机 | 打印操作 |
| 293 | 红叉 | 删除/错误 |
| 294 | 绿勾 | 确认/通过 |
| 2183 | 齿轮 | 设置 |
| 304 | 文件夹 | 打开文件 |
| 163 | 闪电 | 快速操作 |

更多图标可搜索 "Excel FaceID list" 获取完整列表。

---

## 10. 注意事项

1. **.bas 文件必须是 UTF-8 编码** — 构建脚本会自动转为 GBK，不要手动转
2. **Attribute VB_Name 行必须有** — 这是模块名，不能删
3. **Sub 名称不能重复** — 不同 .bas 文件里不能有同名的 Sub
4. **OnAction 必须与 Sub 名一致** — 包括中英文、大小写
5. **构建前必须关闭 WPS** — 否则文件被锁定，构建会失败
6. **Module7.bas 的按钮顺序** — 决定了按钮的排列顺序
7. **FinanceRPA.xlam 不要手动改** — 所有修改在 src/ 下的文件中完成
8. **fix_isaddin.ps1 不要删除** — 构建脚本依赖它修补 isAddin 属性
9. **目标电脑不需要 PowerShell** — 只需双击 install.vbs

---

## 11. 常见问题排查

| 问题 | 原因 | 解决方法 |
|------|------|---------|
| 构建报「文件被锁定」 | WPS 没关闭 | 关闭所有 WPS 窗口，重试 |
| 构建报「Cannot access VBA project」 | 信任设置未开启 | WPS 选项 → 信任中心 → 勾选「信任对 VBA 工程对象模型的访问」 |
| 按钮点击没反应 | OnAction 与 Sub 名不一致 | 检查拼写、大小写是否完全一致 |
| 代码显示乱码 | .bas 文件不是 UTF-8 | 用记事本打开 → 另存为 → 编码选 UTF-8 |
| 按钮没出现 | Module7.bas 未加按钮代码块 | 按[6.2 新增宏+按钮](#62-新增宏--新增按钮)第 2 步添加 |
| 「加载项」选项卡不可见 | WPS 默认隐藏 | 右键功能区 → 自定义功能区 → 勾选「加载项」 |
| 图标不显示 | FaceId 编号 WPS 不支持 | 换一个编号试试 |
| 加载项显示「无效的加载项」 | isAddin 属性缺失 | 确认 fix_isaddin.ps1 存在且构建脚本未跳过该步骤 |
| 安装后按钮不出现 | WPS 未重启 | 关闭 WPS 后重新打开 |
| install.vbs 报错 | WPS 版本过低 | 确认 WPS 2019+，或手动复制 .xlam 到启动文件夹 |

---

## 12. 快速参考卡

```
┌──────────────────────────────────────────┐
│            三步口诀                        │
│                                          │
│   改代码 → 关 WPS → 双击 build.vbs       │
│                                          │
│   双击 pack.vbs → 生成安装包              │
│                                          │
│   复制安装包 → 双击 install.vbs          │
├──────────────────────────────────────────┤
│            按钮添加位置                    │
│                                          │
│   新建模块: 在 src/ 下新建 .bas 文件      │
│   新建按钮: 在 src/Module7.bas 中添加     │
│             （cb.Visible = True 之前）    │
├──────────────────────────────────────────┤
│            三个注意                        │
│                                          │
│   1. .bas 文件必须 UTF-8 编码             │
│   2. OnAction 必须与 Sub 名完全一致       │
│   3. 构建前必须关闭 WPS                   │
└──────────────────────────────────────────┘
```
